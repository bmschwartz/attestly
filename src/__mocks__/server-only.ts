// Mock for server-only module in test environment
// server-only throws when imported outside React Server Components
// This mock allows tests to import server-side modules without error
export {};
